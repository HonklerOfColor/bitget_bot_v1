# Secrets — never commit
config.py
scalper_config.py
.env
.env.*

# Runtime / local data
*.log
spread_learnings.json
bot_state.json
__pycache__/
*.pyc
*.pyo
.DS_Store

# Vendored dependencies (use requirements.txt)
lib/

# Backtest data (generated, can be recreated)
backtest/*.json
backtest/*.png
*.zip
